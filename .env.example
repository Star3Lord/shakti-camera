# Base URL of the BSJ GPS tracking platform. Used for both the login call
# and all subsequent authenticated requests. Per-user tokens are obtained
# at login time and stored in the `bsj_session` cookie — there is no
# project-wide token to configure here.
BSJ_BASE_URL=https://y.gpstracktech.com
